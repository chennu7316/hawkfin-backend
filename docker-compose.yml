services:
  backend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: my-next-app-backend
    env_file:
      - .env
    environment:
      - NODE_ENV=production
      - PORT=${PORT:-4000}
    ports:
      - "4000:4000"
    restart: unless-stopped
